import {Link} from "react-router-dom";

const BrandName = () => {
    return (
        <Link to={"/"}>
            <span className="tracking-wide">AGROW</span>
        </Link>
    );
};

export default BrandName;